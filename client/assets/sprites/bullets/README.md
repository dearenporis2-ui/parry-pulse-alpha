# Bullet Sprites

- `bullet.png` — standard revolver round
- `bullet_super.png` — used automatically when a bullet is deflected/supercharged

Drawn rotated to match travel direction. Recommended size: ~16x16.
Until these exist, bullets render as glowing team-colored dots with a trail —
see `client/js/renderer.js` -> `drawBullet()`.
